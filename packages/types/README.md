# packages/types/

Shared TypeScript/domain types placeholder.

Future responsibilities:

```text
API DTO types
domain enum types
permission types
event types
```

Rule:

```text
Types are not authorization. Runtime checks are still required.
```
