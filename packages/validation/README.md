# packages/validation/

Shared validation schemas placeholder.

Future responsibilities:

```text
API request validation schemas
enum validators
pagination validators
safe string limits
```

Rule:

```text
Validate input before business logic, database query, AI call, or send adapter call.
```
