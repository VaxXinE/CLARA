# packages/ui/

Shared UI components placeholder.

Future responsibilities:

```text
buttons
inputs
layout primitives
status badges
error banners
loading states
```

Rule:

```text
Shared UI must render untrusted content safely and avoid raw HTML by default.
```
