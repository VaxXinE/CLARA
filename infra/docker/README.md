# infra/docker/

Docker-related files placeholder.

Future content:

```text
Dockerfile.api
Dockerfile.dashboard
docker compose helpers
```

Rule:

```text
Do not bake secrets into Docker images.
```
