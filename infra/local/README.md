# infra/local/

Local development infrastructure placeholder.

Future content:

```text
docker-compose.yml
local database config
local test services
```

Rule:

```text
Local infra must not require production credentials.
```
