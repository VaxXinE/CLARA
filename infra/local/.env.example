# Local development placeholders only.
# Do not reuse these credentials outside local development.

POSTGRES_DB=clara_api_dev
POSTGRES_USER=clara_user
POSTGRES_PASSWORD=clara_password_dev_only
POSTGRES_PORT=5432
