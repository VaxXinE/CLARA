# infra/deployment/

Deployment infrastructure placeholder.

Future content:

```text
staging config
production config
deployment manifests
```

Rule:

```text
Production-like config must disable mock auth and demo data by default.
```
